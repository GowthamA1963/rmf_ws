
import rmf_adapter.graph as graph
import inspect

print("min_lane_length:", graph.min_lane_length)
# Create a dummy graph to check waypoint attributes if possible, or just dir the class if exposed
print("dir(graph):", dir(graph))
